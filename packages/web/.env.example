# API
NEXT_PUBLIC_API_URL=http://localhost:3001

# Note: Supabase environment variables have been removed since authentication is no longer used
# If you need to configure Supabase as a service within your environment variables, 
# add the relevant variables in your variable groups through the UI
